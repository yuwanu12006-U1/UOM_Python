pi = 3.1417
radius = float(input("Enter the radius of cylinder (in cm): "))
height = float(input("Enter the height of cylinder (in cm): "))

volume = 2*pi*radius*height
print("Volume = %0.02f cubic centimeters" %(volume))
